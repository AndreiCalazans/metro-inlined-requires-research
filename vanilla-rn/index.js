import App from '../shared-app/src/App';

// Minimal entry: in a real app this would be AppRegistry.registerComponent.
// For bundle-output study we just need to reference App so it is included.
export default App;
